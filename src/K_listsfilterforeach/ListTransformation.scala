package K_listsfilterforeach

object ListTransformation {

  // The input is a list of random element
  // The output should be a list of all strings and integers. However, instead of the strings, the output should contain
  // the // character length of each word in the list. The first element of the resulting list should additionally be
  // truncated. Only make use of methods that are predefined in the List class!

  def apply(list: List[Any]): List[Int] = null
}
