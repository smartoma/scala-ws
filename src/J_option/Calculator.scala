package J_option

object Calculator {

  // This calculator should sum up all values it receives which can be optional.

  // Hint: this problem can be solved rather expressively by using pattern matching and tail recursion.
  // You can annotate a method by @tailrec to ask the compiler to throw a compilation error if a recursive
  // method cannot be optimized for tail recursion.

  def sum(values: List[Option[Int]]) = -1
}
