package B_everythingisobject

object EverythingIsObject {

  // 1. Replace last value (0) with a correct value so that test (test.B_everythingisobject.ObjectSpec) passes.
  // 2. Use more readable way to write '(a).+(b)', something like  'a + b' (infix notation in Scala)
  val three = (1).+(0)

  // Don't touch me.
  val four = three + 1

}
